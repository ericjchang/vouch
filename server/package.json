{
  "name": "chat-app-server",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "main": "server.js",
  "scripts": {
    "server": "node server.js",
    "dev": "NODE_ENV=dev nodemon run server",
    "prod": "NODE_ENV=prod npm run server",
    "test": "NODE_ENV=test jest --runInBand --detectOpenHandles --forceExit"
  },
  "author": "Eric J. Chang",
  "keywords": [
    "chat",
    "chat app"
  ],
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^3.0.2"
  },
  "optionalDependencies": {
    "bufferutls": "*",
    "utf-8-validate": "^5.0.10"
  }
}
